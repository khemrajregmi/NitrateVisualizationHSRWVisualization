To run the program, type "streamlit run app.py"
To activate a virtualenv, type "python -m venv myenv"
To install dependencies, type "pip install -r requirements.txt"
python -m streamlit run app.py

Visualizing Online tool for teaching environment data with user feedback - a guideline
Representing the dataset for teaching environment data with user feedback - a guideline

Visualization and representation of Environment data as an online tool- a guideline

